<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.com.guedes.csgoitemsapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CsgoItemsApp">
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CsgoItemsApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Crate detail activity -->
        <activity
            android:name=".ui.detail.CrateDetailActivity"
            android:exported="false"
            android:label="Crate" />

        <!-- Item detail (skins/stickers/highlights) -->
        <activity
            android:name=".ui.detail.DetailActivity"
            android:exported="false"
            android:label="Detail" />

        <!-- Agent detail activity -->
        <activity
            android:name=".ui.detail.AgentDetailActivity"
            android:exported="false"
            android:label="Agent" />
    </application>


</manifest>